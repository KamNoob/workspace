# 2026-02-12 - Authentication Cooldown

## Anthropic Token Cooldown
- **Status:** 13-minute cooldown applied
- **Cause:** Consecutive 401 auth errors (invalid x-api-key)
- **Profiles affected:** `anthropic:default` and `anthropic:ccode`
- **Action:** Wait 15 minutes for cooldown to expire
- **Note:** `.env` contains Anthropic key (`sk-ant-oat01-...`)

## Resolution
- If cooldown expires and token still fails: Update `ANTHROPIC_DEFAULT_TOKEN` in `.env` with fresh token
