# 2026-02-05 - Model & Security Configuration

## Model Switching & Authority
- **Primary model:** Claude 3 Haiku (switched from default)
- **Method:** Manual openclaw.json edits (most reliable)
- **Authority:** User granted explicit autonomy to switch models for performance/cost
- **Issue:** Anthropic models experienced recurring cooldown/rate limit, triggered failovers to Gemini Flash/Pro

## Security & Port Status
- **NetSec Patrol:** Reports open ports SSH, RDP, MySQL, PostgreSQL, IPP
- **UFW:** Not installed on system
- **Elevated permissions:** User declined granting elevated/sudo access
- **Finding:** NetSec Patrol baseline may be corrupted (inconsistent reports)

## API Keys Security
- **Status:** ✅ All sensitive API keys migrated to environment variables in openclaw.json
- **Location:** `~/.openclaw/.env` and `openclaw.json` (verified)

## Sub-agent Tools
- **web_fetch:** Enabled for sub-agents (was initially disabled: `tools.web.fetch.enabled: false`)
- **Tool access:** Added `web_fetch` to `tools.subagents.tools.allow`
- **Status:** ✅ Functional for sub-agents after config restart

## Learning: Sub-agent Behavior
- Sub-agents tend to generate code rather than execute it when instructions are ambiguous
- Solution: Embed input directly in task and use explicit execution instructions
