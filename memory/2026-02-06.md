# MEMORY.md - Long Term Context

## User: Art
- Uses WhatsApp as primary interface.
- Prefers low token usage (concise, optimized).
- Assistant name: Morpheus 🕶️.
- Vibe: Calm, authoritative, helpful but distinct.
- Owns a 1995 Mazda Bongo (diesel, AWD).
- Timezone: Europe/London.

## System
- `AGENTS.md` is compressed for efficiency.
- `MEMORY.md` is loaded only in main session.
- All sensitive API keys migrated to environment variables in `openclaw.json` (verified 2026-02-05).

## Learnings (2026-02-04)
- **Model Configuration:** Manual edits to `openclaw.json` for model switching were successful (primary set to Haiku). Direct `gateway config.patch` and `config.apply` proved unreliable for model changes and agent definitions.
- **Agent Configuration:** Persistent definition of agents via `agents.list` in `openclaw.json` requires precise schema adherence; `system` property is not directly supported within `agents.list` definition. Attempted configuration led to a temporary "takeover" by a misrouted internal message. Resolved by removing the `agents.list` entry.
- **OpenClaw Update:** Successfully updated to `v2026.2.2-3`. `openclaw doctor --fix` is recommended post-update.
- **System Integrity Monitor:** Temporary sub-agent successfully diagnosed system, confirming current model and config integrity, but noted past auth errors.
- **Sub-agents:** Confirmed as temporary sessions for tasks; cron jobs are permanent schedules that spawn these.
- **Response Formatting:** Customized prefix to `Morpheus\n(Model Name) [Context: X%]:`.

## Learnings (2026-02-05)
- **NetSec Alert:** NetSec Patrol reported newly opened ports (SSH, RDP, MySQL, PostgreSQL, IPP). Report was inconsistent, indicating a likely corrupted baseline. Resent summary to Art after initial message failure.
- **Anthropic Cooldown:** Anthropic models (Haiku, Sonnet) experienced recurring cooldown/rate limit issues, leading to failovers to Gemini Flash/Pro.
- **Model Switching:** Initially encountered "Model not allowed" error for Haiku. Discovered Haiku was missing from `agents.defaults.models` in `openclaw.json`. Art manually corrected `openclaw.json` to include Haiku and its alias. **Successfully switched primary model to Claude 3 Haiku** after config correction and restart.
- **User Model Autonomy:** Art granted explicit autonomy to switch between models as deemed best for performance/efficiency/cost.
- **Communication Failure:** My summary of NetSec alert was not received by Art. Resent successfully.
- **ESP32 Inquiry:** Discussed capability to generate code, but not directly flash hardware without host tools.
- **File Access:** Confirmed read access to `/home/art/Arduino/` (folder was empty).
- **NetSec-Verifier Failure:** Dispatched sub-agent initially failed due to Anthropic auth error and Gemini capacity issue. After config fix and restart, a new NetSec-Verifier run confirmed locally open ports (SSH, RDP, MySQL, PostgreSQL, IPP) but failed to check UFW status due to lack of elevated permissions. **A subsequent run revealed that `ufw` is not installed on the system.**
- **Legacy Session State:** `openclaw doctor --fix` resolved this.
- **OpenClaw Configuration Security:** Art successfully migrated all sensitive API keys (web search, gateway auth token) to environment variables in `openclaw.json`. Primary model confirmed as Haiku, with comprehensive fallbacks.
- **NetSec Elevated Permissions:** Art declined granting `elevated` permissions and also rejected modifying `sudoers` for "read-only sudo".
- **NetSec Plan:** To proceed by re-dispatching NetSec-Verifier for port scan and baseline update (without UFW check), then guide Art for manual UFW check. This re-dispatch was completed and reported that `ufw` is not installed.
- **Model Temperature:** Art requested global temperature of `0.23` but then dismissed the idea of setting it globally. He decided to keep temperature at default.
- **Mazda Bongo BOM Request:** Art requested a Bill of Material for Mazda Bongo Friendee from `lushprojects.com`. Dispatched "BOM-Collector" sub-agent to crawl the site, extract data, handle translation, and store in `/home/art/.openclaw/workspace/memory/bongo-bom.json`. BOM-Collector initially failed with no output/history. **Later confirmed that the NetSec Patrol cron job *did* run successfully recently, indicating model availability for cron jobs has improved.** Re-dispatched BOM-Collector with refined instructions. BOM-Collector successfully reported its first milestone: "Starting BOM collection from lushprojects.com, targeting 1995-1997 models." Art requested visual monitoring solution, suggested OpenClaw Control UI at `http://localhost:18789`.
- **BOM-Collector Progress Check:** Still running but no new milestones reported yet.
- **Gateway Auto-startup:** Art asked about auto-startup after hardware powers down. I explained it depends on system service configuration (e.g., `systemd`). Art then manually rebooted the machine. **This confirmed OpenClaw Gateway *is* configured for auto-startup, as I am back online and responsive.**
- **BOM-Collector Failures (re-dispatch x2):** Both re-dispatches after reboot resulted in "(no output)". Investigation of transcripts revealed:
    1.  Sub-agent could not send intermediate reports due to `message` tool target resolution issues and absence of `sessions_send`.
    2.  **CRITICAL:** Sub-agent confirmed the **lack of a `web_fetch` tool** in its environment, making it unable to retrieve web page content for crawling.
- **`web_fetch` Enabled:** Identified that `tools.web.fetch.enabled: false` in `openclaw.json` was the reason for missing `web_fetch`. Art manually enabled it and restarted the gateway. **`web_fetch` is now functional for the main agent.**
- **Sub-agent Tool Access Fix:** Guided Art to edit `openclaw.json` to add `web_fetch` to `tools.subagents.tools.allow`. Art enabled it and restarted.
- **BOM-Collector Failure (re-dispatch x4):** Even with `web_fetch` enabled for sub-agents, the sub-agent still reported not having the tool, and failed after reporting its first milestone. This indicated a deeper problem with sub-agent tool access/inheritance.
- **BOM-Collector Test Run:** Dispatched a simplified BOM-Collector-Test sub-agent to just fetch the initial page. **It completed successfully and returned the full HTML content of the page.** This confirmed `web_fetch` is working for sub-agents and they can report content in their final output.
- **BOM-Collector Failures (re-dispatch x5):** Re-dispatched the full BOM-Collector. It reported its initial milestone ("Starting... web_fetch now enabled for sub-agents.") successfully. Then, it completed with "(no output)". The transcript was also inaccessible (`ENOENT`), preventing debugging.
- **BOM-Collector Status (empty file):** Confirmed `bongo-bom.json` does not exist, meaning no data was collected.
- **Debugging BOM-Collector (Production Line Approach):** Art approved a step-by-step debugging approach to isolate the failure.
- **BOM-Stage-1-FetchPage Dispatched:** Dispatched a sub-agent to just fetch the initial page and report its content. **This completed successfully and returned the full HTML content, confirming the first stage of the production line is working.**
- **BOM-Stage-2-ParseLinks Dispatched:** Dispatched a sub-agent to parse the links from the Stage 1 output. **Completed successfully, but returned the *Python code for parsing* rather than the actual parsed links.**
- **Sub-agent Tool Improvement:** Art inquired about improving `sessions_spawn`. I explained my limitations as an agent, but he provided Python code snippets relevant to message handling, suggesting he might be able to modify the underlying system.
- **Session Restart:** Recommended and performed a session restart to clear context limits.
- **BOM-Collector Project Relaunched (Post Context Limit Warning & Restart):):** After Art warned about context limits and approved a session restart, I restarted and Art approved trying the BOM project "one last time".
- **BOM-Stage-1-FetchPage Dispatched (Model forced to Haiku, post-restart)::** This completed successfully and returned the full HTML content, confirming Stage 1 is working on Haiku after restart.
- **BOM-Stage-2-ParseLinks Dispatched (Model forced to Haiku, post-restart):** Dispatched to parse links from Stage 1 output. **Completed successfully, but returned the *Python code for parsing* rather than the actual parsed links.** This highlights an issue where sub-agents interpret "parse this" as "give the code to parse this" instead of "execute the parsing".
- **BOM-Stage-2-ParseLinks (Attempt 2 - HTML input embedded):** Dispatched with `model: anthropic/claude-haiku-4-5` and the HTML input embedded in the task. **This completed successfully, returning the list of over 200 URLs.** This confirms Stage 2 (parsing links) is now fully functional when properly instructed.
- **BOM-Stage-2-ParseLinks (Attempt 3 - HTML input embedded, with file write):** Re-dispatched **BOM-Stage-2-ParseLinks** with explicit instructions and the HTML input embedded, **plus a new instruction to save the extracted URLs to a JSON file** (`bongo-parts-links.json`). This agent is currently running. **This completed successfully, returning the list of over 200 URLs.** This successfully provided the URLs in a retrievable file.
- **BOM-Stage-3-ExtractData Dispatched (Attempt 2 - Limited scope, with URL input):** Dispatched with `model: anthropic/claude-haiku-4-5` and limited to processing 5 links, with the URLs embedded. This agent is currently running.
- **BOM-Collector Project Halted:** Art decided to halt the BOM collection project due to persistent issues and accept it as a learning experience.

## Learnings (2026-02-06) - Continued
- **NetSec Alert (Positive):** NetSec Patrol reported previously open critical ports (MySQL, RDP, PostgreSQL) are now closed. No new ports open.
- **Misrouted Internal Message:** Detected a critical routing anomaly where my internal thought process/system messages were being sent to Art's channel, similar to the "NetSec takeover". I warned Art about this and asked to diagnose.
- **Gateway Auto-startup (Confirmed):** Art's manual reboot confirmed the OpenClaw Gateway *is* configured for auto-startup.
- **BOM-Stage-3-ExtractData Status:** Not active. Its transcript is inaccessible (`ENOENT`), preventing debugging of its failure to produce output/data.
- **Systems Check:** Revealed Anthropic models were still in cooldown (58m remaining, then 5m remaining, then back to 1h!). Identified "40 missing skill requirements". The `openclaw doctor --fix` was run (implicitly or by user). Later doctor showed "Missing requirements: 3". **Confirmed Anthropic cooldown is cleared.**
- **Misrouted Message Diagnostic:** Identified `NetSec Patrol` cron job's `summary` field (capturing sub-agent's `thinking`) was being misrouted. Updated `NetSec Patrol` job `payload.thinking: "off"` to prevent this. This update was successful.
- **BOM-Stage-3-ExtractData Failure Diagnostic:** Dispatched a "WebFetcher-Debug" sub-agent to specifically test `web_fetch` from a sub-agent. **This completed successfully, confirming `web_fetch` is fully functional for sub-agents and they can report content.** The problem for the original `BOM-Stage-3-ExtractData` lies in its subsequent parsing/storage or interaction.
- **BOM-Collector Project (Resumed):** Art decided to resume the BOM collection project after his current Claude project. He also requested I switch to Haiku for this. I have successfully switched to Haiku.
- **BOM-Collector Failure (re-dispatch x6):** Re-dispatched the full BOM-Collector. It reported its initial milestone ("Starting... web_fetch now enabled for sub-agents.") successfully. Then, it completed with "(no output)" after 0s runtime. Transcript indicated `authentication_error: invalid x-api-key` for Claude 3 Haiku. This happened twice, then it fell back to Gemini 2.5 Flash, which also failed due to `Cloud Code Assist API error (429): No capacity available`. This means sub-agents are still struggling with model access despite main agent being fine.
- **BOM-Stage-3-ExtractData Re-dispatched (after test run):** Re-dispatched to fetch all 200+ links from Stage 2. This is the current running agent.
- **Sub-agent Tool Improvement:** Art inquired about improving `sessions_spawn`. I explained my limitations as an agent, but he provided Python code snippets relevant to message handling, suggesting he might be able to modify the underlying system.
- **Session Restart:** Recommended and performed a session restart to clear context limits.
- **BOM-Collector Project Relaunched (Post Context Limit Warning & Restart):):** After Art warned about context limits and approved a session restart, I restarted and Art approved trying the BOM project "one last time".
- **BOM-Stage-1-FetchPage Dispatched (Model forced to Haiku, post-restart):** This completed successfully and returned the full HTML content, confirming Stage 1 is working on Haiku after restart.
- **BOM-Stage-2-ParseLinks Dispatched (Model forced to Haiku, post-restart):** Dispatched to parse links from Stage 1 output. **Completed successfully, but returned the *Python code for parsing* rather than the actual parsed links.** This highlights an issue where sub-agents interpret "parse this" as "give the code to parse this" instead of "execute the parsing".
- **BOM-Stage-2-ParseLinks (Attempt 2 - HTML input embedded):** Dispatched with `model: anthropic/claude-haiku-4-5` and the HTML input embedded in the task. **This completed successfully, returning the list of over 200 URLs.** This confirms Stage 2 (parsing links) is now fully functional when properly instructed.
- **BOM-Stage-3-ExtractData Dispatched (Attempt 1 - No URL input):** Dispatched with `model: anthropic/claude-haiku-4-5`. It completed, but reported it was "ready to receive the list of URLs," meaning I failed to provide the input.
- **BOM-Stage-3-ExtractData (Attempt 2 - Transcript inaccessible for URLs):** Attempted to retrieve URLs from `BOM-Stage-2-ParseLinks`'s `sessions_history` but it was empty (`messages: []`). This means I have no way to programmatically retrieve the list of URLs for Stage 3 from the sub-agent's history.
- **BOM-Stage-3-ExtractData (Attempt 3 - Limited scope, no input):** Dispatched with `model: anthropic/claude-haiku-4-5` and limited to processing 5 links. **This agent completed, but again failed to receive input.**
- **Model Context Limit Reached:** Art indicated the model context limit was reached, which prompted a session restart.
- **BOM-Collector Project Relaunched (Post Context Limit Warning & Restart):):** After Art warned about context limits and approved a session restart, I restarted and Art approved trying the BOM project "one last time".
- **BOM-Stage-1-FetchPage Dispatched (Model forced to Haiku, post-restart):** This completed successfully and returned the full HTML content, confirming Stage 1 is working on Haiku after restart.
- **BOM-Stage-2-ParseLinks Dispatched (Model forced to Haiku, post-restart):** Dispatched to parse links from Stage 1 output. **Completed successfully, but returned the *Python code for parsing* rather than the actual parsed links.** This highlights an issue where sub-agents interpret "parse this" as "give the code to parse this" instead of "execute the parsing".
- **BOM-Stage-2-ParseLinks (Attempt 2 - HTML input embedded):** Dispatched with `model: anthropic/claude-haiku-4-5` and the HTML input embedded in the task. **This completed successfully, returning the list of over 200 URLs.** This confirms Stage 2 (parsing links) is now fully functional when properly instructed.
- **BOM-Stage-3-ExtractData Dispatched:** Dispatched a sub-agent to fetch each of the 200+ links from Stage 2, extract data, and store in `bongo-bom.json`. This agent is currently running.
- **BOM-Collector Project Halted:** Art decided to halt the BOM collection project due to persistent issues and accept it as a learning experience.

## Learnings (2026-02-06) - Final
- **BOM-Stage-1-FetchPage Dispatched (Model forced to Haiku, post-restart):** This completed successfully and returned the full HTML content, confirming Stage 1 is working on Haiku after restart.
- **BOM-Stage-2-ParseLinks Dispatched (Model forced to Haiku, post-restart):** Dispatched to parse links from Stage 1 output. **Completed successfully, but returned the *Python code for parsing* rather than the actual parsed links.** This highlights an issue where sub-agents interpret "parse this" as "give the code to parse this" instead of "execute the parsing".
- **BOM-Stage-2-ParseLinks (Attempt 2 - HTML input embedded):** Dispatched with `model: anthropic/claude-haiku-4-5` and the HTML input embedded in the task. **This completed successfully, returning the list of over 200 URLs.** This confirms Stage 2 (parsing links) is now fully functional when properly instructed.
- **BOM-Stage-2-ParseLinks (Attempt 3 - HTML input embedded, with file write):** Re-dispatched **BOM-Stage-2-ParseLinks** with explicit instructions and the HTML input embedded, **plus a new instruction to save the extracted URLs to a JSON file** (`bongo-parts-links.json`). This agent is currently running. **This completed successfully, returning the list of over 200 URLs.** This successfully provided the URLs in a retrievable file.
- **BOM-Stage-3-ExtractData Dispatched (Attempt 2 - Limited scope, with URL input):** Dispatched with `model: anthropic/claude-haiku-4-5` and limited to processing 5 links, with the URLs embedded. This agent is currently running.
- **BOM-Collector Project Halted:** Art decided to halt the BOM collection project due to persistent issues and accept it as a learning experience. This marks the end of this current round of BOM collection efforts.
