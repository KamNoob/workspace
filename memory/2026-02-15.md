# 2026-02-15 - Authentication Fix & System Configuration

## TUI Authentication Issue Resolved
- **Problem:** TUI hanging, auth blocked
- **Root cause:** Expired Claude Pro OAuth token in auth-profiles.json
- **Solution:** Generated fresh token via `ccode auth token`, added as `anthropic:manual` profile
- **Updated:** `lastGood.anthropic` → `anthropic:manual`
- **Status:** ✅ TUI working, authentication successful

## Auth Profile Structure
```json
{
  "profiles": { "provider:name": { "type": "token", "token": "..." } },
  "lastGood": { "provider": "provider:profile-name" },
  "usageStats": { "provider:name": { "lastUsed": timestamp, "errorCount": 0 } }
}
```

## Token Management
- Claude Pro OAuth tokens obtained via `ccode auth login` + `ccode auth token`
- Tokens are account-based, not machine-specific
- Lifespan: Several months to a year before refresh needed

## System Updates Available (Feb 15)
- Ollama: 0.12.7 → 0.16.1 (update requires sudo, not available)
- OpenClaw: 2026.2.14 (update process started)
- Node/npm/Git/Python3: All current ✅

## Environment Configuration
- **File:** `~/.openclaw/.env` (auto-loaded by OpenClaw)
- **Variables:** OPENCLAW_GATEWAY_AUTH_TOKEN, OPENAI_API_KEY, WEB_SEARCH_API_KEY, OPENCLAW_GATEWAY_PORT
- **Permissions:** 600 (user-only, secure)

## OpenAI Quota Issue
- **Status:** API quota exhausted
- **Impact:** memory-lancedb auto-capture/auto-recall failing with 429 errors
- **Resolution:** Disabled autoCapture and autoRecall features
- **Future options:** Add OpenAI credits or switch to cheaper provider (Jina AI, Voyage AI)

## Local Embedding Models
- **memorySearch (workspace):** EmbeddingGemma 300M (GGUF), local, working ✅
- **memory-lancedb (LTM):** OpenAI text-embedding-3-small (hardcoded, cloud)
- **Available locally:** nomic-embed-text (768 dims) via Ollama

## Key Learning: Plugin Limitations
- memory-lancedb plugin is hardcoded to OpenAI only
- Cannot be switched to local embeddings without code modification
- Dimension validation prevents model swapping (1536 vs 768)
- Workaround: Use memorySearch for workspace files + find alternative LTM provider

## System Status
✅ Gateway running
✅ WhatsApp connected
✅ No quota errors (auto-features disabled)
✅ Workspace search operational
