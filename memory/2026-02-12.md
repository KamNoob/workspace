## Claude Model Cooldown Issue (February 12, 2026)
- Both Anthropic tokens (`default` and `ccode`) are under a 13-minute cooldown due to consecutive authentication errors (401 - invalid x-api-key).
- System automatically applied cooldown to prevent spamming.
- `.env` file contains an Anthropic key (`sk-ant-oat01-...`) indicating possible use of an OAuth-style or short-lived token.
- Recommendations suggested:
  1. Wait 15 minutes for the cooldown to expire.
  2. If the token fails again post-cooldown, reauthenticate or update `ANTHROPIC_DEFAULT_TOKEN` in `.env` file.